VIRECTOR_DATA_DIR=/data
# Set to ltx to request the LTX worker. Until an inference backend is
# configured, Virector reports the reason and safely falls back to mock.
VIRECTOR_WORKER_MODE=mock
VIRECTOR_HOST=0.0.0.0
VIRECTOR_PORT=8000
